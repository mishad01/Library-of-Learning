void main() {
  final name =
      "Mishad"; //Final is for declaring its the final value we cant change or re assign them

  var test; //Var means we can override as much as possile so we cant use final var
  final String; //But we can use final in front of Datat type

  //const check; //If we take const then its mendatory to initialize the value

  const cehck2 = "Hello"; //

  final ar = ["hello", "world", "wassup"];
  ar = ["asef", "saf", "dsfa"]; //Here we can assign new value as we gave final
}
